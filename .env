DATABASE_URL="local.db"

# for automatic performance issue detection, enable tracing in `svelte.config.js`
# for readable stack traces, setup org, project, auth token in `vite.config.ts`
PUBLIC_SENTRY_DSN="" # compatible alternatives can be used (e.g. GlitchTip)
