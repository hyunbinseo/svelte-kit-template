DATABASE_URL="db/prod.db"
DATABASE_AUDIT_URL="db/audit.db"

# For automatic performance issue detection, enable tracing in `svelte.config.js`
# SENTRY_DSN="https://..." # compatible alternatives can be used (e.g. GlitchTip)

# (Optional) Add Readable Stack Traces With Source Maps:
# - set `sentrySvelteKit({ org, project })` in `vite.config.ts`
# - set `SENTRY_AUTH_TOKEN` in `.env.sentry-build-plugin` (auto-imported)
# See https://docs.sentry.io/platforms/javascript/guides/sveltekit/manual-setup
